import { Router } from 'express';

const router = Router();

// Define Phase 2 payment routes here
// Example: POST /api/v1/payments/subscribe
// Example: POST /api/v1/payments/webhook

export default router;
