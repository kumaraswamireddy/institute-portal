import { Router } from 'express';

const router = Router();

// Define student-facing routes here
// Example: GET /api/v1/students/profile
// Example: PUT /api/v1/students/profile

export default router;
